[package]
name = "comparison"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1.0.217", features = ["derive", "rc"] }
compactly = { path = ".." }
serde_json = "1.0.138"
csv = "1.3.1"

[dev-dependencies]

scaling = { version = "0.9.0", git = "https://github.com/droundy/scaling" }
bincode = "1.3.3"
zstd = "0.13.2"

[[bench]]
name = "bench"
harness = false
